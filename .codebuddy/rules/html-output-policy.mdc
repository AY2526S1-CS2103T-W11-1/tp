---
description: HTML 输出格式策略规则。约束 harness HTML 工作台、HTML Dashboard 和 HTML 报告只能作为从 JSON/JSONL/Markdown 权威产物派生的人类审查视图，禁止作为 gate、repair、baseline、release 的机器输入。
alwaysApply: true
enabled: true
---

# HTML 输出格式策略规则（Derived Human Review View）

## 1. 核心定位

HTML 只能作为 **派生人类视图**（derived human review view），用于审查、筛选、钻取、定位审核项和展示 evidence。

权威契约仍然是：

- SPEC / Rules / 使用说明等 Markdown 文档；
- `*.plan.json`；
- `run-state.json`；
- `trace.jsonl`；
- `eval-result.json`、`aggregated-result.json`、各 suite JSON；
- `traceability-matrix.md` 与新增的 `traceability-matrix.json`；
- release / staging / approval 的既有 JSON、ack 和 evidence 文件。

## 2. 禁止作为 gate 输入

任何 gate、repair、baseline、release、pre-release、acceptance traceability 或 staging 验证脚本 **不得解析 HTML** 来决定通过 / 失败 / repair / release。

禁止模式：

- 用 HTML DOM 或 HTML 文本作为 `release_allowed` 判定来源；
- 用 HTML 表格代替 `traceability-matrix.json` / `traceability-matrix.md`；
- 用 HTML 页面中的按钮或隐藏字段直接写 Plan、ack、release 决策；
- 修改 gate 逻辑使其依赖 `.html` 文件是否存在，除非该 suite 是专门验证 HTML 派生视图的验收 suite；
- 通过 HTML 生成失败阻断原有 Eval gate，除非显式启用 `--strict-html`。

## 3. 生成与安全要求

所有 HTML 生成脚本必须满足：

1. 非交互执行，可重复生成。
2. 从权威 JSON / JSONL / Markdown / evidence 路径派生。
3. 单文件输出，内联 CSS/JS，禁止外部 CDN、远程 JS、远程 CSS。
4. 所有来自 JSON、Markdown、log、命令输出和用户输入的动态文本必须 HTML escape。
5. evidence 使用相对路径链接，禁止复制大日志正文作为唯一证据。
6. HTML 生成失败默认只产生 warning，不改变原 gate 结果。
7. 只有显式 `--strict-html` 或专门的 HTML 验收 suite 才允许因 HTML 生成失败 hard fail。

## 4. 项目级工作台边界

`.codebuddy/harness-workbench/project-workbench.snapshot.json` 是可重建的项目级快照，不是任务数据库；删除后必须能通过扫描权威产物重建。

`.codebuddy/harness-workbench/project-workbench.html` 只能展示：

- 项目任务总览；
- 任务状态与进度；
- Eval / Traceability / Release evidence 链接；
- Review Queue；
- 审核项定位与续接提示；
- clarification question 表单与 answer 保存结果。

它不得直接修改 Plan、写入 high-risk ack、执行 release、修改 gate 或绕过人工确认。

## 5. Review Response 边界

HTML 中的澄清回答只能通过本地受控 Review Response API 写入：

```text
.codebuddy/harness-workbench/review-responses/<review-id>.json
```

要求：

1. API 必须限制写入白名单目录，拒绝 `..`、绝对路径、符号链接逃逸和任意路径写入。
2. `answer` 文件只能作为用户输入证据和后续 Agent 上下文。
3. `answer` 文件不得替代 SPEC 修订、Plan 重建、high-risk ack、staging approve 或 release approve。
4. 回答导致范围扩大时，必须回到 plan mode / Plan 绑定流程。

## 6. 与其他规则的关系

- 与 `harness-execution-protocol.mdc`：本规则只约束 HTML 展示层；状态机、Plan 绑定、Eval、Release 仍按 harness 协议执行。
- 与 `eval-gate.mdc`：Eval gate 只读取权威 JSON / JSONL / Markdown evidence；HTML 仅增强人类审查。
- 与 `acceptance-id-convention.mdc`：HTML traceability matrix 不替代 ACC-ID 在 SPEC 与测试代码中的强追溯。
