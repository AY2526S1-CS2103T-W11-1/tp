---
description: TDD（技术设计文档驱动）标准开发流程。在进行功能开发、Bug修复、重构等需要修改代码的任务时，必须遵循此流程。
alwaysApply: false
enabled: true
updatedAt: 2026-03-03T14:44:34.561Z
provider:
---

# TDD（技术设计文档驱动）标准开发流程

## 1. 目标

指导 AI 按统一、可控、可追溯的方式完成需求，并将实施过程自动沉淀为文档资产。

## 2. 触发规则

### 必须走完整 TDD 流程（7 阶段）
满足任一条件：
- 变更文件数 ≥ 3
- 涉及公共接口/协议/UI 主流程/资源加载链路
- 影响跨模块协作或存在兼容风险
- 属于功能开发、复杂 Bug 修复、重构改造

### 可走简化流程
同时满足：
- 变更文件 ≤ 2
- 变更行数 ≤ 20
- 无架构变更、无公共接口变更

### 可直接修改（免 TDD）
仅注释、文案、配置微调，且无逻辑影响。

## 3. 标准流程（7 阶段）

1. **需求理解**：明确目标、范围、约束（Unity 版本、语言版本、性能边界）。
2. **文档查阅**：优先查 `Docs`，先文档后代码。
3. **现状分析**：定位代码、调用链、影响范围、根因。
4. **方案设计**：先输出方案，不可直接编码。
5. **用户确认**：方案经确认后再实现。
6. **编码实现**：按方案逐项实施，最小改动、风格一致。
7. **验证检查**：lint/构建/关键回归点检查并总结。

## 4. 强制文档沉淀（AI 必做）

每次任务至少产出以下文档内容（可同文件不同章节）：

- **任务记录**：需求、范围、约束、结论
- **方案记录**：候选方案、最终决策、取舍理由
- **实现记录**：修改清单（文件/改动点/原因）
- **验证记录**：检查项、结果、未决风险
- **回滚说明**：失败回退路径（如适用）

> 文档存放必须遵循 `docs-management` 规则（路径映射、命名、禁止项）。

## 5. 方案输出模板（统一）

在进入编码前，AI 必须输出：

1. 问题与根因
2. 目标与边界（明确不改什么）
3. 修改清单（文件 + 修改类型 + 摘要）
4. 风险与兼容性
5. 验证计划（最少 3 条关键检查）

## 6. DoD（完成定义）

任务完成必须同时满足：

- 无新增 lint/编译错误
- 修改点与已确认方案一致
- 关键路径验证通过
- 文档沉淀已完成并可追溯
- 向用户给出变更总结与风险提示

## 7. 禁止事项

- 不查文档直接改核心逻辑
- 不做现状分析直接给方案
- 未确认方案直接编码（除“直接修改”场景）
- 过度重构或顺手改无关代码
- 只改代码不沉淀文档
- 隐式改变行为且不告知风险

## 8. 例外处理

紧急线上热修可“先修后补”：
- 先最小化止血
- 24 小时内补齐方案与实现文档
- 下个迭代完成结构性修复
